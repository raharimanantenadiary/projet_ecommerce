# ecommerce_api (FastAPI + PostgreSQL)

## Lancer
1) Copier `.env.example` -> `.env` et remplir tes valeurs
2) Installer :
   `pip install -r requirements.txt`
3) Démarrer :
   `uvicorn app.main:app --reload`
